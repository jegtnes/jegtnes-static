<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }} | Alex Jegtnes</title>

  {{>head}}
</head>
<body class="w-100">
  <div class="pa3">

    {{>header}}

    <main>

      {{{ contents }}}

      <ul class="list pl0">

        {{#each collections.blog}}
        <li>
          <h2><a href=/{{this.path}}>{{{this.title}}}</a></h2>
          <p><time datetime="{{this.date}}">{{this.date}}</time></p>
          <p>{{{ excerpt this.excerpt this.contents 200 }}}</p>
        </li>
        {{/each}}

      </ul>
    </main>

    {{>footer}}
  </div>
</body>
</html>
